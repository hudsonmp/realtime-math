# Core dependencies
torch>=2.2.0
transformers>=4.50.0
timm>=0.9.0,<1.0.0  # Compatible with transformers 4.50.0+
peft>=0.15.0
huggingface_hub>=0.20.0

# Optional: GPU-optimized quantization (recommended for RUBIK Pi)
bitsandbytes>=0.45.0

# Alternative quantization options (uncomment as needed):
# optimum>=1.16.0  # HuggingFace Optimum for GPTQ/AWQ
# onnxruntime-gpu>=1.16.0  # ONNX Runtime for cross-platform

